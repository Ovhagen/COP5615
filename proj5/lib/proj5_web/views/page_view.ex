defmodule Proj5Web.PageView do
  use Proj5Web, :view

  def index("index.html", assigns) do
    # start broadcasting
    # timer = 1000
    # Proj5.FetchData.update_all(timer)
  end

  # def render("transactions.html", conn) do
  #   # start broadcasting
  #   timer = 1000
  #   Proj5.FetchData.update_all(timer)
  # end
end
